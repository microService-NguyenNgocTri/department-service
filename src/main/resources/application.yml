server:
  port: 9002

spring:
  application:
    name: STUDENT-SERVICE

eureka:
  client:
    register-with-eureka:
      value: true
    fetch-registry:
      value: true;
    service-url:
      defaultZone: http://localhost:9000/eureka
    instance:
      hostname: localhost